# ref: https://github.com/viaduct-ai/kustomize-sops
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: secret-generator
  annotations:
    config.kubernetes.io/function: |
        exec:
          path: ksops
secretFrom:
  - metadata:
      name: firefly-env
    envs:
      - ./config/config.secret.env
  - metadata:
      name: data-importer-env
    envs:
      - ./config/data-importer.secret.env
