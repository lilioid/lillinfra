hostname = mx1.z9.aut-num.de

passdb {
  driver = passwd-file
  args = /app/conf/dovecot-extra.passwd
}

passdb {
  driver = ldap
  args = /app/conf/dovecot-ldap.conf.ext
  mechanisms = plain login
  skip = authenticated
}

