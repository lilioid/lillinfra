FROM docker.io/dovecot/dovecot:2.4.5-dev

USER root
RUN apt-get update &&\
    apt-get install -y --no-install-recommends rspamd &&\
    rm -rf /var/cache/apt/
USER vmail
ADD --chown=vmail rspamc.sh /usr/local/libexec/sieve/rspamc

